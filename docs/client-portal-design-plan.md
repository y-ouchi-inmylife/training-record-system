# クライアントポータル デザインプラン

**位置づけ**: 設計方針文書（実装前）
**対象読者**: 実装担当（自分）／ 将来のレビュアー
**成果物**: このドキュメント1件。コードは書かない。実装は本プラン承認後、別作業で行う。

---

## 1. 方向性

犬の成長を記録するアルバムとして設計する。飼い主が毎週開くたびに「うちの子、また新しいことができるようになった」という発見が届く場所であり、業務ツールでも管理帳でもない。**写真が画面の主役**で、色と組版はそれを引き立てる額縁として、**明るく元気だが写真の邪魔を絶対にしない静けさ**を同時に持つ。

---

## 2. カラー

Bootstrap 既定の `#fd7e14`（.bg-client-nav）は破棄する。以下 6 色を新設する。

コントラスト比は WCAG 2.2 に基づき机上計算（1.4.3 テキスト = AA 4.5:1 / AAA 7:1、1.4.11 非テキスト = 3:1 が基準）。**鮮やかな色を細線・小さな図形・フォーカスリングに割り当てない規律を、初版の tennis 事故（1.4〜1.5:1）を反省点として組み込む**（詳細は §7）。

| トークン | HEX | 用途 | 主要組み合わせのコントラスト（机上計算） | なぜこの色か |
|---|---|---|---|---|
| `paper` | `#F5F3EC` | ページ背景 | ink on paper = **15.5:1** ✓AAA | 「アルバムのページ」の温かみを持つ紙色。AI 既定の #F4F1EA より僅かに緑寄り・落ち着き寄りで、既定パターンから外す |
| `mat` | `#FFFFFF` | カード面・写真の周辺・入力欄の面 | ink on mat = **17.4:1** ✓AAA<br>cobalt on mat = **8.29:1** ✓AAA<br>ember on mat = **6.27:1** ✓AA | 純白。写真アルバムの伝統的なマット色そのもの。**写真の色が最も正確に見える色**を選ぶことが、写真をアプリの主役に据える設計判断そのもの |
| `ink` | `#0F1A2E` | 本文・見出しのインク・細線・フォーカスリング外側 | 全ての明色上で 15:1 以上<br>ink on cobalt = **2.10:1** ✗（cobalt 面には mat 文字を使う）<br>ink on ember = **2.77:1** ✗（ember 面にも mat 文字を使う） | 黒ではなく深い紺（`cobalt` と同族色相）。テキストの主色。65% で副次テキスト、45% でメタ情報として不透明度で階層をつける。cobalt との親和で palette 全体が青軸で統一される |
| `cobalt` | `#1E45B0` | 日付ブロック帯・主要 CTA・リンク・成功トースト・フォーカスリング外側 | cobalt on paper = **7.40:1** ✓AAA（テキスト）<br>cobalt on mat = **8.29:1** ✓AAA<br>mat on cobalt（button 文字）= **8.29:1** ✓AAA | 犬関連サービスが集中する暖色帯（茶・オレンジ・緑）から意図的に離脱。犬の写真は多くが暖色系のため、cobalt は**補色的に効き**、写真を邪魔せず引き立てる。テキスト・面・線・フォーカスリング、どの用途でもコントラスト十分 |
| `persimmon` | `#F0592B` | タグ・「新着」/「今日」ピル・強調点（**面 + ink 文字ペア専用**） | ink on persimmon = **5.11:1** ✓AA<br>persimmon on paper = **3.04:1** ✗テキスト（✓ 3:1 UI 大面積）<br>persimmon on mat = **3.40:1** ✗テキスト（✓ 3:1 UI）<br>mat on persimmon = **3.40:1** ✗テキスト | 熟した柿の色。cobalt の**補色関係**で並べると画面が引き締まる。**明色地上で 3:1 に届かないため、線・小さな図形・テキスト色には使えない**（初版 tennis と同じ罠を避ける明文化）。用途は**面 + ink 文字のペア専用**。持ち場は §2-1 で厳格に規定する |
| `ember` | `#B2311C` | エラーテキスト（**mat 面上のみ**）・破棄アクション面（.btn-danger 等）。**面として使う場合の文字は `mat`（白）に限る** | ember on mat = **6.27:1** ✓AA（テキスト）<br>ember on paper = **5.60:1** ✓AA（テキスト）<br>mat on ember（面上の文字） = **6.27:1** ✓AA<br>**ink on ember = 2.77:1** ✗（ember 面に ink 文字を載せない） | 深い煉瓦色。persimmon と同系の暖赤で palette 内で違和感がないが、明度を大きく落とすことでテキスト用途に耐える。Bootstrap 既定 `#dc3545` は青みが強く palette から浮くため使わない |

**カード面の背景色差（要注記）**: `paper` × `mat` のコントラスト比は **1.11:1**。**色の差でカード・入力欄の輪郭を作ることは不可能**。境界は罫線または影で作ることが必須（実装仕様は §2-1「カード境界の作り方」参照）。純白 `mat` を選んだ理由（写真の色が最も正確に見える）は維持するが、その代償として境界実装が必須になる依存関係をここに明示する。

### 2-0. 写真と喧嘩させない規律（このパレットの中核ルール）

アルバム方向で最も陥りやすい失敗は、鮮やかな色を面で多用して**写真の色と衝突させる**こと。以下を設計上の禁止事項として明文化する。

- **鮮やかな色（`cobalt` / `persimmon` / `ember`）は「構造」に使う** — 日付ブロック帯、主要 CTA、タグ、状態表示、エラーの持ち場に限定
- **写真の周囲・カード本体・写真グリッドの背景には彩度の高い色を絶対に敷かない**。カード面は常に `mat` (#FFFFFF)、ページ背景は `paper` (#F5F3EC) の無彩色に固定
- 純白のマットは、写真の色が最も正しく見えることが選定理由。**アプリの主役は写真であり、UI は額縁**
- vivid な色を「面」として使うのは**日付ブロック帯 (`cobalt`)** と**成功トースト (`cobalt`)** の 2 用途のみ。タグ (`persimmon`) は「点」として、小さく・少なく使う

### 2-1. 色の使用制約（アクセシビリティ由来）

机上計算の結果、以下を設計上の**禁止事項**として明文化する。実装時に迷わないための約束。

#### persimmon の禁止用途（初版 tennis の教訓を踏襲）
- **persimmon を線・小さな図形・アイコン・テキスト色に使わない**（明色地上で 3:1 未満、フォーカスリング・状態表示として機能しない）
- **持ち場は「面 + ink 文字」のペア専用**（`persimmon` 塗りの上に `ink` 文字を載せた「タグ・ピル」の形でのみ使う）
- persimmon 面上に white / mat 文字を載せない（3.40:1 で AA 未達）

#### ember の配置制約
- **ember をテキストとして使うとき、`mat` 面上（カード内・入力欄内・入力欄直下の invalid-feedback）にのみ配置する**
- `paper` 上でも 5.60:1 で AA 通過するため配置可能だが、**エラーは常にカード内で語られるべき**（ユーザーが「どこの入力に問題があるか」を明確に見せる文脈が要る）ため、`mat` 面限定を規約とする
- ページ全体に対するエラーが必要な場合は、まず `mat` のバナー（カード）で包み、その中に ember テキストを載せる

#### ember を面として使う場合の文字色（.btn-danger 等）
- **文字は `mat`（白）に限る**（mat on ember = 6.27:1 ✓AA）
- **`ink` を ember 面に載せない**（ink on ember = 2.77:1、AA 不合格。可読性が担保できない）
- **SCSS 実装時の注意**: Bootstrap の `color-contrast($color)` は `$color-contrast-dark` と `$color-contrast-light` の間で自動選択する。`$color-contrast-dark: $ink` を設定している場合、ember 面に対しても color-contrast が ink を選ぶ可能性がある — ember の輝度によっては閾値近傍で「ink が選ばれる」誤動作が起き得る。実装時に **`.btn-danger` の文字色を明示的に `$mat`（白）に上書き**するか、`$min-contrast-ratio` を 4.5 以上に設定して color-contrast が確実に mat を選ぶよう調整する

#### フォーカスリング（設計書の中核）
初版で tennis に割り当てて失敗した部分。今回は**二重輪郭**で「どの背景色の上でも見える」ことを机上で保証する。

- **定義**: `box-shadow: 0 0 0 3px $mat, 0 0 0 5px $ink;`
  - 内側 3px = `mat` (#FFFFFF)
  - 外側 2px（合計 5px - 3px）= `ink` (#0F1A2E)
- **各背景での可視性**（机上）:
  - `paper` 上: 外側 ink リングが **15.5:1** で可視。内側 mat リングは背景に溶ける
  - `mat` 上: 外側 ink リングが **17.4:1** で可視。内側 mat リングは背景に溶ける
  - `cobalt` 面上（主要 CTA の focus 時）: **内側 mat リングが 8.29:1 で可視**、外側 ink リングは cobalt 上で 2.10:1 と溶けるが、内側 mat が担保するため合格
  - `persimmon` 面上: 内側 mat リングが 3.40:1 で 3:1 通過、外側 ink リングも 5.11:1 で可視
- 単色のフォーカスリングでは「明色地」と「vivid CTA 面」を両立できないため、**二重輪郭が必然**

#### 鮮色同士の隣接禁止（境界が消える組み合わせ）

鮮やかな色同士を並置したときのコントラストを机上計算した結果、いずれも 3:1 に届かない：

| 隣接組み合わせ | 比 | 判定 |
|---|---|---|
| cobalt × persimmon | **2.44:1** | ✗ |
| persimmon × ember | **1.84:1** | ✗ |
| cobalt × ink | **2.10:1** | ✗ |

以下を禁止事項として明文化する：

- **鮮やかな色（`cobalt` / `persimmon` / `ember`）を直接隣接させない**（境界が視認できず、UI コンポーネント間の分離が失われる）
- 特に **`cobalt` の日付ブロック帯の上に `persimmon` のピル・タグを置かない**（2.44:1 で境界が視認できない）
- 鮮色を並べる必要がある場合は、**必ず `mat` (#FFFFFF) または `paper` (#F5F3EC) を 1px 以上の枠または余白として挟む**（mat / paper が担う「無彩の緩衝帯」の役割）
- **`ink` も `cobalt` 面上では使えない**（2.10:1）。cobalt 面のテキストは `mat` のみ

具体的な設計判断への影響：
- 日付ブロック（cobalt）に「今日」ピル（persimmon 面）を重ねたい場合は、ピルの下に **2px 以上の mat 枠を敷く**（例: `border: 2px solid $mat` を pill に付ける）
- タグを cobalt CTA の隣に置く場合は、CTA とタグの間に最低 8px の余白（mat/paper が見える隙間）を確保する
- 破棄アクションボタン（ember 面）を主要 CTA（cobalt 面）の直近に置く場合、両者の間に mat/paper の余白を必ず入れる

#### cobalt 面の境界（写真グリッド周辺の視認性）
- 日付ブロックの cobalt 帯は、隣接するカード本体（mat）とのコントラスト差 **8.29:1** で十分に見える
- 追加の外枠・落ち影は不要

#### カード境界の作り方（paper × mat = 1.11:1 への対応）

`paper` (#F5F3EC) と `mat` (#FFFFFF) のコントラスト比は **1.11:1**。色差でカード・入力欄の輪郭を作ることは不可能なため、以下の方針で境界を作る。

**WCAG 1.4.11 との関係（判定）**: カード内部にはテキスト・写真という**それ自体で意味を伝える情報**が入っている。したがってカード境界は「コンポーネントを識別する唯一の視覚情報」ではなく、**装飾的境界**に分類される。WCAG 1.4.11 の 3:1 を厳密に満たすことは要求されない。ただし輪郭がまったく見えないと視覚的な「まとまり」の情報が失われるため、以下を規約として実装する：

**カード・入力欄の境界仕様**:

- **細罫線**: `border: 1px solid rgba(15, 26, 46, 0.08);`（`ink` を 8% 不透明度で使用。paper 上での実効コントラストは約 1.1:1 で 3:1 未達だが、下の影と併用することで境界を perceivable にする）
- **落ち影**: `box-shadow: 0 1px 3px rgba(15, 26, 46, 0.08), 0 4px 12px rgba(15, 26, 46, 0.06);`（`ink` を 6〜8% で 2 段影。写真アルバムの「印画紙を貼った」感覚に寄せる）
- **合成の効果**: 罫線単独では 3:1 を満たさないが、罫線 + 影の組み合わせで**輪郭を知覚できる状態**を作る。装飾的境界としての設計判断

**依存関係の明示**（設計者への注意）:

- 純白 `mat` を選んだ理由（写真の色が最も正確に見える）を維持する代償として、**カード境界の実装が必須**になる
- 実装時に「罫線か影のどちらかを外して単純化する」判断をしてはいけない — 単独ではどちらも境界の役割を果たせない
- 将来的にカード面色を変える判断をする場合（例: mat → 薄いクリーム色に変更）は、その色と paper のコントラスト比を再計算し、色差だけで境界が作れるかを再判定する

**入力欄も同じ扱い**: `.form-control` は `mat` 背景で入力欄の輪郭を持たないと入力可能領域が不明瞭になる。Bootstrap 既定の border（`$input-border-color`）を上記の細罫線値に置き換える。フォーカス時は §2-1 のフォーカスリング（mat + ink 二重輪郭）が発火する

#### 成功系メッセージ（`tennis` の代替）
- Bootstrap 既定の緑は導入しない。前案で使っていた `tennis` は新パレットに存在しない
- **成功トーストは `cobalt` 面 + `mat` 文字で表現する**（cobalt は palette 内で「肯定・確定」を担う唯一の強い色）
- 既存の Bootstrap `.alert-success` は変数上書きで cobalt 系に置き換わる
- cobalt 面は paper 上でコントラスト 7.40:1 のため輪郭は明瞭、外枠追加は不要

---

## 3. タイポグラフィ

**Web フォントは一切使わない。見出しも本文もシステム日本語スタックで組む**。方針を実測結果に基づき修正した（前案では Zen Maru Gothic を採用していたが撤回した — 詳細は下記）。

### 候補比較（丸みのある日本語書体）— **全て不採用**

前案では「丸みのある日本語 Web フォント」を見出しに使う方針で、以下 4 候補を比較していた。**実装後の実測により方針を撤回**。比較表は履歴として残す。

| 候補 | Google Fonts | 動的サブセット (`unicode-range`) | 想定実効サイズ（JP、1 ウェイト） | ウェイト展開 | モバイル本文の可読性 | 判定 |
|---|---|---|---|---|---|---|
| Zen Maru Gothic 500 | ○ | ○ | 前案では実効 40〜80KB と見積もり | 300 / 400 / 500 / 700 / 900 | 高い | **不採用**（下記） |
| M PLUS Rounded 1c 500 | ○ | ○ | 実効 50〜90KB | 100〜900（8 段階） | 高い | 不採用（同じ理由） |
| Kosugi Maru 400 | ○ | ○ | 実効 40KB | 400 のみ | 中 | 不採用（同じ理由） |
| Klee One 400/600 | ○ | ○ | 実効 60〜100KB | 400 / 600 | 中 | 不採用（同じ理由） |

**撤回の理由（実測値）**: Zen Maru Gothic を実装してブラウザで初回読み込み時の転送量を実測した結果、以下だった：

| 内訳 | サイズ |
|---|---|
| CSS（`css2?family=Zen+Maru+Gothic` 応答） | 29.0 kB |
| woff2 × 5 サブセット | 61.9 kB |
| **合計** | **90.8 kB** |

前案の「実効 40〜80KB」見積もりは **CSS 分（29KB）を含めていなかった**。29KB の CSS は動的サブセット `@font-face` 宣言が 100 個以上並ぶ構造的なコストで、**使用箇所を絞っても減らない**。計測ページは文字数が少なく、飼い主名・犬名・トレーナーのノート本文などが入る実運用では 100KB を超える見込み。

読者はモバイル主体・週 1 回程度の訪問という前提に対して、100KB 前後の追加転送は釣り合わない。「使用箇所を絞れば実効サイズが小さい」という論理が CSS 分を数えないと成立しない、という**見積もり側の甘さ**が根本原因（§7 自己批評参照）。

他候補（M PLUS Rounded 1c / Kosugi Maru / Klee One）も同じ Google Fonts の仕組みを使うため、**CSS 分の 20〜30KB は同様に発生**する。個別書体固有の可読性議論以前の理由で全候補が不採用になる。

### 書体の役割（最終決定）

| 役割 | 書体 | 採用可否 | 理由 |
|---|---|---|---|
| 見出し・本文・UI テキストのすべて | システム日本語スタック（下記「フォントスタックの定義」参照） | **採用**（唯一） | Web フォント無し、ダウンロードコストゼロ。見出しと本文の階層は**書体差ではなく、ウェイト・サイズ・letter-spacing・色**で作る（下記「型スケール」）|
| Zen Maru Gothic / M PLUS Rounded 1c / Kosugi Maru / Klee One | 各種 Web フォント | **不採用** | 上記実測理由 |
| Noto Sans JP | Web フォント | **不採用** | 一貫性は上がるが、モバイル読者に対して 100KB+ のコスト（Zen Maru Gothic と同じ罠） |
| 数字・データ用 mono | JetBrains Mono 等 | **不採用** | 桁揃えは `font-variant-numeric: tabular-nums` で解決。mono の投入は AI 既定の「テック風の格好つけ」でこのブリーフに合わない |

### フォントスタックの定義

前案のスタック `-apple-system, BlinkMacSystemFont, "Hiragino Kaku Gothic ProN", "Yu Gothic", "Meiryo", sans-serif` には Windows 特有の問題がある：

- **Windows で `"Yu Gothic"` を CSS で明示指定すると「Yu Gothic Light（ウェイト 300）」が当たる**既知の問題があり、極端に細く描画される
- 本文サイズ（1rem）で Light ウェイトになると可読性が低下

したがって以下のスタックに変更する：

```
system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
"Hiragino Sans", "Hiragino Kaku Gothic ProN",
"Yu Gothic Medium", "Meiryo", sans-serif
```

**各要素の役割**:

| 要素 | 対象 OS / ブラウザ | 効果 |
|---|---|---|
| `system-ui` | モダンブラウザ全般 | OS のシステム UI フォントを取得。Windows なら Segoe UI（Latin）+ Yu Gothic UI（JP はグリフ単位フォールバック、**Yu Gothic Light 問題を回避**）／ macOS/iOS なら San Francisco + Hiragino Sans／ Android なら Roboto + Noto Sans CJK |
| `-apple-system`, `BlinkMacSystemFont` | 古い macOS / iOS の Safari / Chrome（`system-ui` 未対応時） | SF フォント fallback |
| `"Segoe UI"` | 古い Windows Chrome | Latin フォントの明示 fallback |
| `"Hiragino Sans"` | macOS 10.11+ / iOS 9+ | 現行 macOS/iOS の日本語フォント（新しい名前） |
| `"Hiragino Kaku Gothic ProN"` | 古い macOS / iOS | ヒラギノ角ゴ（旧名） |
| `"Yu Gothic Medium"` | Windows | **`"Yu Gothic"` ではなく `"Yu Gothic Medium"` を明示**。「Medium」は独立ファミリ名でウェイト 500 で描画される — Light 問題が発生しない |
| `"Meiryo"` | 古い Windows | Yu Gothic 未搭載環境での fallback |
| `sans-serif` | 絶対 fallback | 全て失敗時の汎用 sans |

**判断の根拠**:
- `system-ui` を最初に置くことで、モダン環境では OS がグリフごとに最適なフォントを選ぶ（Windows なら Latin=Segoe UI・JP=Yu Gothic UI Regular で Light 問題を発生させない）
- 明示的な JP フォント指定は「モダン `system-ui` が効かない旧ブラウザ」用の保険。その場合でも `"Yu Gothic Medium"` を先に置くことで Yu Gothic Light を避ける
- 見出し（ウェイト 700）でも同じスタックを使う。system-ui が効く環境では Yu Gothic UI Bold が当たる。旧環境では Yu Gothic Medium ファミリに対して browser がボールドを合成（synthesized bold）— 完全ではないが機能的

### 型スケール（書体差を失った分をウェイト・サイズ・letter-spacing・色で補う）

Bootstrap の rem を維持する。行間は日本語のため 1.5〜1.8。**見出しと本文の階層を書体で作れなくなったため、以下の 4 手段で明確化する**：

1. **ウェイトの落差**: 見出し = 700、本文 = 400（Bootstrap 既定より大きな段差）
2. **サイズの落差**: Display / H1 / H2 を前案より一段大きく引き上げる
3. **字間**: 見出しに letter-spacing を効かせて存在感を作る
4. **色による階層**: 見出しに `cobalt` を使い、本文の `ink` と分ける（cobalt on paper = 7.40:1 ✓AAA、cobalt on mat = 8.29:1 ✓AAA）

| 役割 | サイズ | ウェイト | letter-spacing | 文字色 | 行間 | 用途例 |
|---|---|---|---|---|---|---|
| Display | **2.75rem**（前案 2.5rem から拡大） | **700** | **0.02em** | **`cobalt`** | 1.3 | ログイン画面のワードマーク、詳細画面の日付見出し |
| H1 | **2rem**（前案 1.75rem から拡大） | **700** | **0.01em** | **`cobalt`** | 1.4 | ダッシュボードの挨拶 |
| H2 | **1.5rem**（前案 1.375rem から拡大） | **700** | **0.01em** | **`cobalt`** | 1.4 | セクション見出し（使用は最小限） |
| H3 / カード内見出し | 1.125rem | 700 | 0 | `ink` | 1.5 | 現時点で使用箇所なし。将来カード内・詳細画面内で複数のサブ見出しが必要になった場合（例: 記録詳細内で「写真」「ノート」を明示的なサブ見出しで区切る等）に使う想定。スタイル定義は用意しておく |
| Body | 1rem | 400 | 0 | `ink` | 1.75 | 本文・ノート |
| Meta | 0.875rem | 400 | 0 | `ink` 65% | 1.6 | 時刻・件数・キャプション |
| Micro / Eyebrow | 0.75rem | **700** | **0.08em** | `ink` 65% | 1.4 | 「最新のトレーニング」等の小見出し。字間を広く取り、太字で強調（前案の「大文字化・字間広げは行わない」制約は丸ゴシック採用時のもの。今回はシステム sans なので字間を広げて OK） |

**設計判断の付記**:
- **見出しの色 = cobalt**: これで見出しが本文（ink）と一目で区別できる。cobalt は日付ブロック帯・CTA・リンクでも使う「構造の色」として palette 内で意味が統一される
- **Display と H1 の色も cobalt**: 詳細画面の日付見出し「8月2日（金）のトレーニング」も cobalt になり、日付ブロック帯（cobalt）との視覚的つながりが生まれる
- **letter-spacing は日本語でも効く**（Latin 用語ではあるが、和文でも字送りが広がって落ち着いた印象になる。過度に広げると読みにくくなるため 0.01〜0.02em に抑制）
- **カード内見出し (H3) の色は ink のまま**: カード内には既に cobalt の日付ブロック帯があるため、追加で cobalt を投入すると同一カード内で cobalt が濫用され視覚的な焦点が分散する。また cobalt 面同士の隣接や、cobalt に隣接する鮮色（persimmon）を誘発しやすくなり、§2-1 の禁止事項に抵触するリスクがある。原則として **1 カード内に cobalt は日付ブロック帯だけ**にとどめる

### 数字の扱い

初版では `font-variant-numeric: tabular-nums` を body 全体に適用していたが**範囲が広すぎた**（和文中の「3 匹」「12 月」まで桁位置が固定され、意図しない副作用があった）。**適用範囲を以下の要素に限定する**：

- **日付ブロックの日の数字**（`.date-block .day` 相当）— 大きく表示するため桁位置固定が読みやすさに直結
- **記録件数の表示**（例: 「これまで **N** 回のトレーニング」の N）— 数字が増減するため桁位置固定
- **時刻**（詳細画面「10:00」）— 桁揃えで読みやすい
- **入力欄の郵便番号・電話番号**（`input[type="tel"]` / `#postal_code` / `#phone1` 等）— 入力中の桁ずれ防止

body 全体には適用しない。実装時は上記の要素に対して個別に `font-variant-numeric: tabular-nums` を効かせる（`.num-tabular` ユーティリティか、要素セレクタで直接指定）。

---

## 4. レイアウト

各画面のワイヤーフレームを ASCII で示す。特にダッシュボードは現状案と改善案を並べる。

### 4-0. 全画面共通のナビゲーション

各画面のワイヤーフレームに入る前に、複数画面にまたがる共通要素を先に確定する。以下の各画面（§4-1 以降）ではナビ内部の詳細は繰り返さず、この節を参照する。

#### 適用範囲

| レイアウト | ナビの有無 | 備考 |
|---|---|---|
| `layouts.client`（認証済み: ダッシュボード・記録詳細） | あり | 本節の仕様を適用 |
| `layouts.client-public`（ログイン・パスワード設定・トークン切れ） | なし | 招待メール由来の pre-auth 画面。画面自身のワードマークがヘッダを兼ねる（§4-3 等）|
| `layouts.client-intake`（事前入力フォーム・完了） | なし | pre-auth の1タスク完結画面。回遊要素を置かない |

#### 見た目

| 項目 | 値 |
|---|---|
| 実装クラス | `.c-nav`（Bootstrap の `.navbar` と組み合わせ）|
| 背景色 | `mat`（純白）|
| 文字色 | `ink`（`.c-nav` 内で `--bs-navbar-color: $ink` を設定）|
| 罫線 | 下端 1px 幅、`rgba(15, 26, 46, 0.08)`（ink 8%、`$border-color` と同値）|
| 影 | 付けない |

**判断の理由**：
§1「写真が主役、UI は額縁」の徹底。段階2 の暫定実装では `forest`（暗色）でナビ帯を作っていたが、画面上部に黒帯が乗って「管理画面の記号」になり、飼い主のためのアルバムという方向性から外れていたため撤回した。カード（罫線 + 影）とは扱いを分け、ナビは「面より薄い区切り線」でページ上端を示すに留める。文字色を ink（構造の色）にすることで、cobalt（§2）は主要 CTA・見出し・日付ブロック帯・リンクにだけ使うという色の役割分担も守れる。

#### ワードマーク（左端）

- `config('app.client_portal_name')` から供給する（§9-1 と同じキー）
- Blade にハードコードしない

**判断の理由**：
プロダクト名は現時点で未確定（§9-1）であり、将来の変更に耐える構造を担保するため。段階4-2 以降のログイン画面ワードマークと供給元を統一することで、名称変更時の修正箇所が config 1 か所に収束する。

#### 右端（ユーザー表示 + ログアウト）

- ユーザー名 + `さん` を `navbar-text` として表示
- ログアウトは **文字リンク**（Bootstrap `.btn-link .btn-sm`）
- 枠線ボタン・塗りボタンにしない

**判断の理由**（§6 のログアウト行と整合）：
飼い主にとってログアウトは主要な行為ではない。週1回訪れて記録を見る場所であり、退出を促す設計にしない（§6「主要な行為ではないため面積を落とす」）。装飾レベルを主要 CTA と揃えると視覚重量が競合するため、文字リンクに落とす。

#### 実装上の禁則

- `!important` を使わない。段階2 の `.bg-client-nav` は Bootstrap の `navbar-dark`（白文字前提）を打ち消すため `!important` を多用していたが、段階4-6 で Blade から `navbar-dark` を除去したことで、Bootstrap 5.3 の CSS カスタムプロパティ（`--bs-navbar-color`、`--bs-navbar-brand-color` 等）の素直な上書きだけで意図した色になる。詳細度を上げる必要がなくなった
- Blade 側で `navbar-dark` / `bg-client-nav` / `text-white` / `btn-outline-light` を使わない（すべて段階2 の暫定コード。段階4-6 で除去済み）
- `.c-nav` は `layouts.client` の nav 要素にのみ適用する。pre-auth レイアウトには適用しない

### 4-1. ダッシュボード — 現状（問題の可視化）

```
モバイル（〜360px）
┌──────────────────────────────┐
│ ▓▓ orange nav bar            │
│ ○○ さん  [ログアウト]        │
├──────────────────────────────┤
│ ○○さん、ようこそ              │
│                              │
│ ┌─ メディア（N件）──────────┐│  ← カード1
│ │▓▓▓▓ 縦スクロール（270px）▓│↕│
│ │ [img][img][img][img]      │↕│  ← スクロール1
│ │ [img][img][img][img]      │↕│
│ └─────────────────────────┘│
│                              │
│ ┌─ 記録（N件）─────────────┐│  ← カード2
│ │ 日付|担当1|担当2|内容    ││
│ │ ▓▓▓ 縦スクロール（200px）│↕│  ← スクロール2
│ │ ...                      │↕│
│ └─────────────────────────┘│
└──────────────────────────────┘

問題:
- 縦スクロール箱が2つ縦に並ぶ（入れ子スクロール）
- 「写真がいつのものか」がテーブルとの間で失われる
- 記録0件時は「メディアはありません」「記録はありません」の
  2つの空箱が並ぶだけ
```

### 4-2. ダッシュボード — 改善案

**構造の変更**:
1. メディアと記録テーブルを解体し、**セッションカードの縦フィード**に統合する
2. 1回のトレーニング = 1枚のカード。カード内に日付・写真数枚・ノート冒頭を同居させる
3. ページ全体のスクロールのみ。カード内スクロールは持たない
4. 直近1件を大きな「今週のトレーニング」カードとして先頭に、それ以外を「これまでの記録」フィードに

```
モバイル（〜575px）  改善案
┌──────────────────────────────┐
│ [wordmark]      メニュー・out │  ← 細めのナビ、ワードマーク中心
├──────────────────────────────┤
│                              │
│ ○○さん、こんにちは            │  ← システムサンス 700 2rem cobalt
│ ┃ (犬の名前・写真の予約領域) │  ← 現状は非表示、将来差し込み
│                              │
│ ── 最新のトレーニング ────   │  ← eyebrow micro type
│                              │
│ ╔══════════════════════════╗│  ← 主カード（mat 面）
│ ║▓▓ 8月 2日（金） ▓▓▓▓▓▓▓▓║│  ← 日付ブロック（cobalt "横" 帯 + mat 文字）
│ ║                          ║│      = 署名要素（§5、モバイル形態）
│ ║  ┌────────┐┌────────┐    ║│
│ ║  │ photo  ││ photo  │    ║│  ← 写真は 2 列で折り返し
│ ║  └────────┘└────────┘    ║│      （幅の制約で必然）
│ ║  ┌────────┐┌────────┐    ║│
│ ║  │ photo  ││ photo  │    ║│
│ ║  └────────┘└────────┘    ║│
│ ║  +3                      ║│
│ ║                          ║│
│ ║  今日は「まて」の姿…     ║│
│ ║  続きを読む →            ║│
│ ╚══════════════════════════╝│
│                              │
│ ── これまでの記録 ─────     │
│                              │
│ ┌──────────────────────────┐│
│ │▓▓ 7月 19日（木） ▓▓▓▓▓▓▓││  ← 日付ブロック横帯
│ │                          ││
│ │  [t][t]                  ││
│ │                          ││
│ │  水路のそばで安定して…   ││
│ │  続きを読む →            ││
│ └──────────────────────────┘│
│ ┌──────────────────────────┐│
│ │▓▓ 7月 12日（木） ▓▓▓▓▓▓▓││
│ │                          ││
│ │  [t][t]                  ││
│ │  おすわり・まてを反復…   ││
│ │  続きを読む →            ││
│ └──────────────────────────┘│
│  ...                         │
└──────────────────────────────┘

デスクトップ（1024px+）  改善案
┌────────────────────────────────────────────────────────────┐
│ [wordmark]                          メニュー · ○○さん · out │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ○○さん、こんにちは                                          │
│  ┃ (犬の名前・写真の予約領域)                              │
│                                                            │
│  ── 最新のトレーニング ──────────────                       │
│                                                            │
│  ╔══════════════════════════════════════════════════════╗  │
│  ║┃ 8月│ ┌────────┐┌────────┐┌────────┐  +2         ┃  ║ │  ← 写真は横並び
│  ║┃ 2  │ │ photo  ││ photo  ││ photo  │              ┃  ║ │
│  ║┃ 金 │ └────────┘└────────┘└────────┘              ┃  ║ │
│  ║┃    │                                              ┃  ║ │
│  ║┃    │ 服従訓練                                     ┃  ║ │
│  ║┃    │ 今日は「まて」の姿勢を安定させる練習を…      ┃  ║ │
│  ║┃    │ 続きを読む →                                 ┃  ║ │
│  ╚══════════════════════════════════════════════════════╝  │
│                                                            │
│  ── これまでの記録 ─────────────────                        │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐  │
│  │┃7月 │ [thumb][thumb] 呼び戻し                       │  │
│  │┃19  │ 水路のそばで放して安定して戻ってきた…         │  │
│  │┃木  │                                                │  │
│  └──────────────────────────────────────────────────────┘  │
│  ...                                                        │
└────────────────────────────────────────────────────────────┘
```

**「過去のすべてを見る」ページネーションは今回のスコープに含めない**。理由：現状ダッシュボードは全件を単一クエリで受け取っており、既存の運用ペース（週次投稿 × 想定期間 1〜2 年で 50〜100 件）ではフィード全表示で成立する。ページネーションは実測で UX が壊れた時点で扱う独立課題として切り離す。前案のワイヤーフレームにあった「過去のすべてを見る →」導線は削除した。

**記録0件（空状態）**:

```
┌──────────────────────────────┐
│ [wordmark]      メニュー・out │
├──────────────────────────────┤
│                              │
│ ○○さん、こんにちは            │
│                              │
│ ─── ここに届きます ─────     │
│                              │
│ ┌──────────────────────────┐│
│ │                          ││
│ │  最初のトレーニングが     ││
│ │  記録されると、この場所に ││
│ │  日付・写真・ノートが     ││
│ │  順番に届きます。         ││
│ │                          ││
│ │  ┌───────────┐           ││  ← カードの見本（半透過）
│ │  │┃ダミー … │           ││
│ │  └───────────┘           ││
│ │                          ││
│ └──────────────────────────┘│
└──────────────────────────────┘
```

「メディアはありません」「記録はありません」の 2 つの空箱を、**1 つの説明カード**にまとめる。装飾のイラストは入れず、ダミーカードで「ここに何が来るか」を先に見せる。

### 4-3. ログイン画面

```
┌──────────────────────────────────────────┐
│                                          │
│              (paper 背景)                │
│                                          │
│                                          │
│         ┌────────────────┐               │
│         │  [wordmark]    │               │  ← システムサンス 700 2.75rem cobalt
│         │   Display大      │               │     ロゴ画像は撤去、
│         │                │               │     テキストのみ
│         └────────────────┘               │
│                                          │
│         ┌────────────────────┐           │
│         │ (mat カード)        │           │
│         │                    │           │
│         │  メールアドレス     │           │
│         │  ┌──────────────┐  │           │
│         │  └──────────────┘  │           │
│         │                    │           │
│         │  パスワード        │           │
│         │  ┌──────────────┐  │           │
│         │  └──────────────┘  │           │
│         │                    │           │
│         │  [ ログインする ]  │           │  ← cobalt 面 + mat 文字
│         └────────────────────┘           │
│                                          │
│         トレーナーからの案内             │  ← 現在の
│         メールでログインします           │     「© ○○トレーニングカンパニー」
│                                          │     を置き換える
│                                          │
└──────────────────────────────────────────┘
```

**ポイント**:
- ミカン絵の `client-portal-logo.png` を撤去し、ワードマーク（テキスト）に置き換える提案。ミカンは犬の文脈と噛み合わない
- 現行の "© ○○トレーニングカンパニー" TODO 行を、意味のある一文（ログイン時の状況説明）に差し替える
- Bootstrap 既定の `.form-control` 縮小 padding（app.scss の指定）は client.scss には持ち込まない。指入力に最適化して 0.5rem 上下を確保する

### 4-4. トレーニング記録詳細

```
┌──────────────────────────────────────────┐
│ [wordmark]        メニュー · ○○ · out    │
├──────────────────────────────────────────┤
│                                          │
│  ← ダッシュボードへ                       │  ← 「戻る」ではなく行き先
│                                          │
│  8月2日（金）のトレーニング              │  ← システムサンス 700 2.75rem cobalt
│                                          │
│  ┃ 山田 花子 ／ 佐藤 太郎               │  ← 「担当1/担当2」廃止
│  ┃ 10:00                                │
│                                          │
│  ── 写真と動画 ──────────                │
│                                          │
│  [photo][photo][photo]                   │  ← 均等グリッド、スクロールなし
│  [photo][video][photo]                   │
│                                          │
│  ── トレーナーからのノート ──            │  ← 「トレーニング記録」廃止
│                                          │
│  今日は「まて」の姿勢を安定させる練習を  │
│  中心に行いました。前回よりも待機時間が  │
│  長くなり、集中が続くようになっています。│
│                                          │
│  ── やったこと ─────                     │  ← 「トレーニング内容（詳細）」廃止
│                                          │
│  15分の基礎の反復のあと、公園の遊具      │
│  そばで環境刺激を上げて練習しました。    │
│                                          │
└──────────────────────────────────────────┘
```

**ポイント**:
- 現状の "基本情報 / メディア / トレーニング記録" の 3 カード積みを解体。日付・トレーナー・時刻は**ヘッダーブロック**に統合し、装飾のカードで囲まない
- メディアはカード内グリッド。件数の縦スクロールは持たない（数十件を超える運用は現時点で想定外）
- 「未記入」の代わりに、そのセクション自体を非表示にする（空セクションを見せない）

**種別（training_type）タグを詳細画面から削除する理由**（前案では表示していたが撤回した）:

1. **情報ではなく装飾になる**: 種別は `training_types` マスタで「事前相談 / トレーニング / その他」の 3 種のみ。大半の記録が「トレーニング」になるため、ほぼ全画面に同じタグが並ぶ。§7 の「装飾を情報に見せかけない」規律に反する
2. **§2-1 の禁止事項に抵触する**: 詳細画面のタイトル（`c-detail-title`）は cobalt であり、その直下に persimmon のタグを置くと**鮮色が隣接**する（cobalt × persimmon = 2.44:1）。設計書内で §2-1 と §4-4 が矛盾していた
3. **ダッシュボードと扱いを揃える**: ダッシュボード §4-2 でも同じ理由で種別タグを廃止済み。同じ記録が 2 画面で違う見え方をすると飼い主が混乱する

### 4-5. 事前入力（intake）フォーム

```
┌──────────────────────────────────────────┐
│                                          │
│         事前入力情報                       │
│                                          │
│  ● 基本情報 ── ○ 連絡先 ── ○ 完了         │  ← 3点シェブロン
│                                          │     完了を含めて全体像を先に見せる
│                                          │
│  ┌────────────────────────────────────┐  │
│  │ (mat カード)                        │  │
│  │                                    │  │
│  │  基本情報（1/2）                    │  │
│  │                                    │  │
│  │  お名前（必須）                     │  │
│  │  ┌────────┐ ┌────────┐              │  │
│  │  │ 姓      │ │ 名     │              │  │
│  │  └────────┘ └────────┘              │  │
│  │                                    │  │
│  │  ふりがな                           │  │
│  │  ┌────────┐ ┌────────┐              │  │
│  │  │ せい    │ │ めい   │              │  │
│  │  └────────┘ └────────┘              │  │
│  │                                    │  │
│  │  生年月日            性別           │  │
│  │  ┌──────────┐        ┌──────────┐   │  │
│  │  └──────────┘        └──────────┘   │  │
│  │                                    │  │
│  │                     [ 次へ ]        │  │
│  └────────────────────────────────────┘  │
│                                          │
└──────────────────────────────────────────┘
```

**ポイント**:
- 現状の progress bar + badge の 2 段構成を、**3 点シェブロン**（基本情報→連絡先→完了）に一本化。「あと何段階か」を送信前から把握できる
- ラベルは `.form-label-fixed`（140px 固定）から離脱し、**ラベル上・入力下の縦積み**を全域で採用。モバイルでの視線移動が短い
- Bootstrap 既定の `.form-control` は client.scss で padding を戻し、モバイル指入力に適したサイズ（最低 44px 高さ）を確保
- 郵便番号検索や住所自動入力の細部は、**成功時にトースト表示、失敗時にインラインメッセージ**とし、`alert()` は使わない（実装フェーズの約束）

### 4-6. 事前入力完了

```
┌──────────────────────────────────────────┐
│                                          │
│         ✓                                │  ← cobalt 面 + mat 文字の小さな丸（アイコンではない）
│                                          │
│         送信が完了しました                 │  ← システムサンス 700 1.5rem cobalt
│                                          │
│         トレーナーからご連絡があるまで、  │
│         少々お待ちください。              │
│                                          │
│                                          │
└──────────────────────────────────────────┘
```

「このウィンドウを閉じてください」は突き放しすぎる。次に何が起きるかを 1 行で書く。

### 4-7. パスワード設定

```
┌──────────────────────────────────────────┐
│                                          │
│         [wordmark]                        │
│         パスワードを決めます              │
│                                          │
│         ○○@example.com にログイン        │  ← 「メールアドレス: ○○」の
│         するためのパスワードです          │     ラベル型を廃止、文にする
│                                          │
│         ┌──────────────────────┐          │
│         │ パスワード            │          │
│         │ ┌──────────────────┐  │          │
│         │ └──────────────────┘  │          │
│         │                      │          │
│         │ 8文字以上、大文字・   │          │  ← 補助文（既存文言の
│         │ 小文字・数字・記号を  │          │     整形のみ）
│         │ それぞれ1つ含めます   │          │
│         │                      │          │
│         │ パスワード（確認）    │          │
│         │ ┌──────────────────┐  │          │
│         │ └──────────────────┘  │          │
│         │                      │          │
│         │  [ 設定する ]         │          │
│         └──────────────────────┘          │
│                                          │
└──────────────────────────────────────────┘
```

**パスワード強度メーターは今回のスコープから外す**。理由：デザイン改良ではなく機能追加であり、JavaScript を要する。`resources/js/app.js` は不可触という制約のもとで安定した置き場所がない。強度可視化は独立課題として、JS 実装方針が決まった時点で別枠で扱う。

### 4-8. 無効なトークン（password-setup / intake 共通パターン）

```
┌──────────────────────────────────────────┐
│                                          │
│         [wordmark]                        │
│                                          │
│         この案内は開けません              │  ← $title
│                                          │
│         リンクの有効期限が切れています。  │  ← $message（既存の変数）
│                                          │
│         ── 次にできること ────            │  ← 追加ブロック
│                                          │
│         ・トレーナーに新しい案内を        │
│           お願いしてください              │
│         ・別のメールが届いていないか      │
│           確認してください                │
│                                          │
└──────────────────────────────────────────┘
```

「なぜ入れないか」だけで終わらせず、「次にどうすればよいか」を必ず 1 ブロック分添える。$title と $message はコントローラから渡ってくる既存の変数を尊重し、周りだけデザインで補強する。

---

## 5. シグネチャー

**日付ブロックを維持する**（初版からの引き継ぎ）。**表示形態は画面幅で切り替える**（下記「表示形態の分岐」参照、実装後の実機確認で必要と判明した）。色は cobalt、文字は mat、書体はシステムサンス（本文書体）ウェイト 700。mat 文字 on cobalt 面は 8.29:1 で AAA。カードの本体は mat 面で、写真とノートが並ぶ。

### 5-1. 表示形態の分岐（デスクトップ / モバイル）

| 画面幅 | 形態 | 配置 | 表記例 |
|---|---|---|---|
| **≥ 576px**（Bootstrap `sm` 以上） | **cobalt 縦帯** | カードの**左端**、日本語**縦組み** | `8月` / `2` / `金`（縦に 3 段） |
| **< 576px**（Bootstrap `sm` 未満、スマートフォン想定） | **cobalt 横帯** | カードの**上部**、日本語**横 1 行** | `8月 2日（金）`（横一列） |

日の数字はどちらの形態でも Zen Maru Gothic ではなくシステムサンス 700、大きめの font-size で組む。

### 5-2. なぜ分岐が必要か

初版〜前案では**モバイルでも縦帯を維持**する想定でワイヤーフレームを描いていたが、**実装後の実機幅（400px 前後）確認で以下の問題が判明**：

- モバイル幅ではカード本体（date-block の右）の写真が **2 列以上に折り返し**、カードが縦に伸びる
- 日付ブロックの高さはカード全体に追随（`.c-session` の `align-items: stretch`）するため、cobalt の縦帯も一緒に縦に伸びる
- 結果、**文字のない cobalt の柱**がカード左に長く残り、「日付が視覚のリズムを作る」という本節の意図が「青い柱が並ぶ装飾」の印象に変わる

**根本原因**: モバイル幅では写真の折り返しにより「日付ブロックとカード本体の縦横比」が縦帯の想定を超える。縦帯は「カード高さ ≒ date-block 高さ」を前提としており、写真が横 1 列で収まる幅（≥ 576px）でのみ成立する。

### 5-3. 横帯にしても §5 の効果が損なわれない理由

- **「この写真はいつのものか」を語る効果**: カード上部の横帯は写真グリッドの直前に日付を置くため、視線の流れ「日付 → 写真」が自然。むしろ写真より先に日付が来ることで「これはいつのトレーニング」がより明確に伝わる
- **スクロール時のリズム効果**: 縦フィード全体で見たとき、cobalt の横帯が**カード上端に等間隔で並ぶ**ため、リズムは水平線として維持される（縦帯が縦に連続していたリズムの水平版）。「成長の連なり」を反復で示す構造は残る
- 縦帯・横帯どちらも「cobalt の色帯 + 日本語 + カード内の一意な位置での反復」という §5 の 3 要素を満たす

### 5-4. デスクトップ側の維持

デスクトップ（≥ 576px）は写真が横 1 列に収まり、date-block の高さがカード全体の高さを支配する。縦帯が縦組み日本語で機能する条件が揃っているため、**デスクトップ側は縦帯を維持**する。

### 5-5. 別要素に変更しない判断の理由（引き継ぎ）

- アルバム的方向でも「この写真はいつのものか」を写真の隣で語る役割は変わらず必要
- 別案として「写真オーバーレイの日付」「縦線タイムライン装飾」「各セッションで日付ブロックの色を回転」を検討したが、
  - 写真オーバーレイは記録によって写真が無い場合成立せず
  - 縦線タイムラインは装飾要素（線）の追加を必要とし、
  - 色の回転（cobalt→persimmon→ember...）は色が持つ意味を消し「装飾のためだけの色使い」になる（制約5 に抵触）
- 日付ブロックを維持する選択が最も少ない装飾で最大の構造効果を生む

### 5-6. 日本語表記の維持

「8月 / 2 / 金」の日本語表記を維持する（初版で決定した論理はそのまま有効）。英語 `AUG / 2 / FRI` は欧米カレンダーチップ UI の借用で「一目で他と区別する要素」というこの節の主張と矛盾する、という理由が引き続き有効。**モバイル横帯でも英語表記に切り替えない**（「8月 2日（金）」の日本語横書き）。

### 5-7. 構造で成立させる規律（制約5・§2-1 との整合）

- 装飾（封筒・ハンコ・破線・足跡・パーティー装飾・シール）は一切入れない
- `cobalt` の色帯 + システムサンス（ウェイト 700）の日本語 + 反復 の 3 要素だけでシグネチャーを成立させる
- 犬の名前・写真を将来受け取る余地は、日付ブロックの**下段**（縦帯時）／**右端**（横帯時）に確保する（現状は非表示、将来「タロウ」等が入る）
- 前案の「forest 塗りの小丸（新着ピップ）」は撤回済み（当時の色 forest / 小丸 は新パレットに存在しない）。新着表示は必要になった時点で `persimmon` タグ + ink 文字（「今日」「新着」の 2 文字ピル）として §2-1 の規約下で追加する
- **§2-1 制約との整合**（横帯形態でも維持）：
  - cobalt 面の文字は mat のみ（ink は 2.10:1 で使えない）— 8月・日・曜日いずれも mat で 8.29:1
  - 鮮色同士を直接隣接させない — 横帯（cobalt）と写真グリッド（背景 mat）の間は横帯の下 padding で分離。persimmon 等の vivid 面は横帯周辺に配置しない
  - カード境界は罫線 + 影の両方（横帯化しても `.c-session` の border と box-shadow は不変）

---

## 6. 文言の書き換え表

報告された全項目に加え、実装で見つけた同種のものを含める。

| 現状 | 書き換え案 | 理由 |
|---|---|---|
| `トレーニング記録閲覧`（title / navbar-brand / login h4） | **プロダクト名（未確定 — §9-1）**。Blade にベタ書きせず `config('app.client_portal_name')` 等の外部供給を経由する | 機能名を製品名にしている。プロダクトの人格がない。かつ将来の改称に耐える |
| `© ○○トレーニングカンパニー` | **トレーニング提供会社名（未確定 — §9-2）**。Blade にベタ書きせず、config または DB からの外部供給を経由。**未供給時はフッター行ごと非表示**にし、レイアウトが崩れない構造にする | TODO のまま本番公開。運営会社が確定するまで、無理に著作権表記を出さない。将来複数社対応にも耐える |
| `○○さん、ようこそ` | `○○さん、こんにちは` | 「ようこそ」は入口の言葉。週次で会う場所には過剰。毎週の挨拶を作る |
| `担当1` `担当2` | `トレーナー`（複数名はカンマ区切り／中点区切り） | 内部スキーマの露出。飼い主は「担当の何番目か」を知りたくない |
| `現在このメディアは表示できません（変換状態: processing）`（alert） | `この写真/動画は準備中です。少ししてから開いてみてください。`（インライン表示、トーストまたはカード上のバッジ） | 内部状態語彙の露出。`alert()` 自体もモバイル UX として不適切 |
| `未記入` | セクション自体を非表示（ノートが無ければ「トレーナーからのノート」ブロックごと出さない） | 「未記入」はフォーム語。飼い主に見せる場所ではない |
| `—`（テーブル内の空値） | 記録カード化により大半が消滅。残る場所（担当が 0 名など）は「トレーナー未定」と明示、または非表示 | ハイフンは意味が伝わらない |
| `トレーニング記録（N件）` | 見出し「これまでの記録」、件数はサブテキスト「これまで **N** 回のトレーニング」 | UI 語彙（括弧+件数）を挨拶文にほぐす |
| `メディア（N件）` | 廃止。写真・動画は各セッションカード内に統合。カード内では「+3」表記 | 「メディア」自体が業界語。飼い主は「写真」「動画」で考える |
| `このウィンドウを閉じてください`（intake 完了） | `送信が完了しました。トレーナーからご連絡があるまで、少々お待ちください。` | 突き放し。次に何が起きるかを伝える |
| `ステップ 1/2: 基本情報`（intake カードヘッダ） | `基本情報（1/2）` | 「ステップ」は補助語。主役はセクション名 |
| `1. 基本情報` `2. 連絡先`（intake バッジ） | 3 点シェブロン `基本情報 → 連絡先 → 完了` | 完了を含む全体像を先に見せる |
| `戻る` `« 戻る` | `ダッシュボードへ` | 抽象的な方向ではなく行き先を書く |
| `トレーニング記録詳細`（詳細画面 h2） | `8月2日（金）のトレーニング`（日付を主役に） | 「詳細」はメニュー語。中に入っている読者に「詳細」と言う必要はない |
| `基本情報`（詳細画面カードヘッダ） | 廃止。ヘッダーブロックに統合 | 管理画面用フォーム語 |
| `トレーニング内容（詳細）` | `やったこと` | 人の言葉。「今日」は過去記録を見る飼い主に対して成立しない（同一日に複数記録が入る可能性を含めて日を主語にしない） |
| `トレーニング記録`（詳細画面カードヘッダ） | `トレーナーからのノート` | 誰が誰に対して残したものかを名づける |
| `パスワードの設定`（password-setup サブタイトル） | `パスワードを決めます` | 「設定」より「決める」の方が飼い主の主体的な動作 |
| `メールアドレス: ○○@○○` + プレーンテキスト表示 | `○○@example.com にログインするためのパスワードです`（文中） | ラベル+値の管理画面型を、宛先を含めた 1 文にほぐす |
| `メディアはありません` | セクション自体を非表示（記録詳細では既存の「この記録のメディアはありません」も同様） | 空箱を見せない |
| `トレーニング記録はありません` | 空状態カード（§4-2）に統合 | 単独の「ありません」表示を廃止 |
| `郵便番号を正しく入力してください（7桁）`（alert） | インラインエラー表示（入力欄下） | `alert()` を廃止し、既存の invalid-feedback パターンに寄せる |
| `住所が見つかりませんでした`（alert） | 都道府県欄付近にインライン注記 | 同上 |
| `住所検索に失敗しました`（alert） | 同上 | 同上 |
| `○○ さん`（ナビバー） | `○○ さん` は維持。ただしテキスト色・字間・サイズを調整して「作業ツールのユーザー表示」感を薄める | 表現は同じで扱いを変える |
| `ログアウト`（ナビバーの btn-outline-light） | 文字リンク化（アイコンなし、装飾なし）。主要な行為ではないため面積を落とす | 主要 CTA と同じ視覚重量にしない |

---

## 7. 自己批評

方向性が「便り」から「アルバム・成長記録」へ、配色が中間色から vivid へ、書体は明朝から丸ゴシックを検討したが最終的に**システム日本語スタックのみ**（Web フォント無し）へ着地した。この新方向に対して**どこが凡庸か・何を変えたか・何を失ったか**を書く。

### 新方向で AI が反射的に出す答え（それを避けた記述）

1. **「ビビッドな色で楽しさを出す」ブリーフに対して AI が反射で選ぶのは、子供向けアプリのようなカラフル配色**（primary red-yellow-blue、または pastel rainbow）。今回はこれを避け、**cobalt + persimmon の補色 2 色**に絞った。色数を絞って構造に集中させる規律で、幼稚さを回避しつつ「元気さ」を作る。

2. **絵文字・イラスト・手描き風装飾・足跡・パーティー要素で「楽しさ」を作るのは AI 既定パターン**。制約5 で明文化・すべて却下。楽しさは色・組版・余白・構造で作る。この規律は初版の「便り」でメタファーを装飾に堕させないと決めた原則の延長線上にある。

3. **犬シルエット・足跡アイコン・骨のモチーフ**は制約5 で明文化・却下。すべての犬関連サービスに存在する定番であり、独自性がない。犬の存在は、日付ブロック下段の予約領域（将来の犬名・犬写真）で構造として表現する。

4. **丸ゴシックとして真っ先に選ばれがちなのは M PLUS Rounded 1c または Kosugi Maru**。前者は Google Fonts で最頻出、後者は 1 ウェイトのみ。→ 使用頻度が抑えめで階層作りに耐える **Zen Maru Gothic** を一度選んだが、後日**実測サイズが 90.8KB**と判明して撤回（詳細は下記 13 番）。丸みのある書体自体は全て却下し、システム日本語スタックのみに着地した。

5. **鮮やかな色を全面に敷くと写真が死ぬ**。アルバム方向で最も陥りやすい失敗。→ 「色は構造に、面は静か」を **§2-0 の規律として明文化**。vivid colors を面として敷くのは**日付ブロック帯 (`cobalt`) と成功トースト (`cobalt`) の 2 用途だけ**に絞り、カード本体・写真グリッドの背景は `mat` (#FFFFFF) と `paper` (#F5F3EC) の無彩色に固定。純白のマットは写真の色を最も正しく見せるため、写真アルバムの伝統をそのまま踏襲する。

6. **成長のグラフ・進捗バー・スター評価・アチーブメントバッジ**は「成長記録」ブリーフで AI が出しがちなゲーミフィケーション装飾。→ 数値可視化には走らない。トレーニングの「回数」ではなく「その日何をしたか」の積み重ねを見せるアプリの本質を尊重する。ダッシュボードの hero も「進捗指標カード」ではなく、単に最新のセッションカードそのもの（初版から維持）。

### 初版から失ったもの（自覚しておくべきトレードオフ）

7. **静けさ・大人の落ち着き**: 初版の「明朝 + meadow + forest」の組み合わせが持っていた**書物のような品**は消えた。今は「cobalt + システムサンス（ウェイト・サイズ・色による階層）」の**元気さ**を主人格にした。方向転換の代償として明示的に受け入れる。ブリーフが変わった以上、「静けさ」を守ることは新方向と両立しない。

8. **中間色の絶妙さ**: `meadow #EEF1E8` の緑寄り中間色は palette の独自性を担っていた。今の `paper #F5F3EC` はより一般的な紙色に近く、その独自性は弱くなった。ただし palette 全体の独自性は cobalt + persimmon の補色配置で別のかたちで確保している。

9. **書体固有の個性**: 初版で Zen Old Mincho（明朝）を選んだ理由は「手紙を書いた人の肌温度」を出すため、改訂版で Zen Maru Gothic を選んだ理由は「丸みで元気を出す」ためだった。**今回はどちらも失い、システム日本語スタックの中庸な字形になった**。書体の個性が担っていた「らしさ」は、色（cobalt）・ウェイト（700）・letter-spacing・サイズ で代替する（§3 型スケール）。書体で語れる情緒は失うが、モバイル読者に対して 90.8KB の追加転送を強いる方が UX の損失として大きい、と判断した。

### 初版の失敗を教訓として組み込んだ点

10. **アクセシビリティを事前計算した**。初版では `tennis` をフォーカスリング・新着ピップに割り当てて 1.52:1 / 1.39:1 で WCAG 1.4.11 未達を後から指摘され撤回した。今回は palette 設計段階で全ての主要組み合わせのコントラストを机上計算し、`persimmon` が明色地上で 3.04〜3.40:1 と初版 tennis と同じ罠を持つことを事前に発見。**用途を「面 + ink 文字のペア専用」に限定**して罠に陥らない構造にした（§2-1）。「vivid な色は面としてのみ、ink 文字と組んで使う」という規約は、tennis 事故の一般化した教訓。

11. **フォーカスリングは二重輪郭**。単色では「明色地」と「vivid CTA 面」を両立できないという初版で学んだ事実を、机上計算で確認した上で `box-shadow: 0 0 0 3px $mat, 0 0 0 5px $ink` の二重輪郭を採用。cobalt 面上でも persimmon 面上でも合格することを事前保証。

12. **tabular-nums を body 全体に適用しない**。初版では body に適用して和文の数字にまで作用させていた。今回は日付・件数・時刻・電話番号の要素に限定（§3 数字の扱い）。適用範囲を絞ることも「装飾を最小限にする」規律の一部。

13. **Zen Maru Gothic の実効サイズを 40〜80KB と見積もったが、ブラウザ実測で 90.8KB（CSS 29KB + woff2 61.9KB）だった**。見積もりの立て方が甘かった — Google Fonts の CSS 分（動的サブセットのために `@font-face` 宣言が 100 個以上並ぶ 29KB）を数えていなかった。CSS 分は使用箇所を絞っても減らない構造的なコストで、他の丸ゴシック候補（M PLUS Rounded 1c / Kosugi Maru / Klee One）も同じ Google Fonts の仕組みを使うため 20〜30KB は同様に発生する。→ 全候補を不採用に切り替え、システム日本語スタックのみで組む方針にした。**初版で `tennis` のコントラストを実測せず 1.5:1 の失敗をした構造と同じ**（数字を確かめずに設計判断をした）。今回も「見積もり値をブラウザで測る」プロセスを設計段階に組み込まなかったのが原因。次回は Web フォント採用の判断で**必ず実測してから決める**規則を追加する。

14. **モバイル向けワイヤーフレームを「写真が横 1 列に収まる前提」で描いていた**。実装後、実機幅（400px 前後）で確認して初めて、写真が 2 列以上に折り返し・カードが縦に伸び・日付ブロックの cobalt 縦帯も一緒に間延びし、「日付が視覚のリズムを作る」（§5）意図が「青い柱が並ぶ装飾」に変質してしまうことが判明した。→ **モバイル（< 576px）は cobalt 横帯 に切り替え**、日付をカード上部に横 1 行で置く形態に修正した（§5-1）。デスクトップ側は縦帯を維持。**tennis の 1.5:1（数字を確かめず色を選んだ）・Zen Maru Gothic の 90.8KB（数字を確かめずサイズを見積もった）と同じ構造**の失敗（**実物・実測を確かめずに設計判断をした**）。次回はモバイル向けワイヤーフレームを描く際に**実機幅で写真の折り返しを検証してから決める**規則を追加する。

### 別種の失敗: 判断変更時の点検漏れ（cross-section consistency）

上記 10〜14 は「実物・実測を確かめずに設計判断をした」失敗だったが、以下は**別種の失敗**である。「1 箇所を直したときに、同じ判断が及ぶ他の箇所を点検しなかった」失敗。

15. **ダッシュボード（§4-2）から種別タグを廃止した際、同じ判断が及ぶ他の箇所を点検せず、設計書内に複数の自己矛盾を残した**。具体的には以下 5 箇所が古い前提（種別タグを表示する）のまま残っていた：
    - §3 型スケール表: H3 の用途例に「セッションカード内のトレーニング種別」
    - §3 表下注記: H3 を ink にする理由として「トレーニング種別を cobalt にすると」
    - §4-2 構造の変更: 「カード内に日付・種別・写真数枚・ノート冒頭を同居させる」
    - §4-4 ワイヤーフレーム + ポイント欄: 「服従訓練 ← タグ（persimmon 面 + ink 文字）」・「日付・種別・トレーナー・時刻はヘッダーブロックに統合」
    - §6 文言書き換え表: 「（日付と種別を主役に）」

    この失敗は 10〜14 の「実測を怠った」パターンとは構造が異なる。数字は正しく測っていたし、設計判断（種別タグ廃止）自体も正しかった。**問題は「その判断が波及する範囲を検索しなかった」こと**。§4-2 だけを直して満足し、他節を確認しなかった。実装（`show.blade.php`）が §4-4 と食い違ったことで、レビュー時に初めて発覚した。

    再発防止として以下の**規則を追加**する：**「設計上の判断を変更したときは、その判断が参照されている全箇所を検索して点検する」**。具体的な運用：
    - 廃止・変更するトークン、要素、規約について、その名前で設計書全体を grep する
    - 見つかった全参照が新方針と整合しているかを確認する
    - 整合していない箇所は同じ turn で全て修正する（部分修正で止めない）
    - 実装（コード）と設計書の両方を対象とする

### 候補外だった選択肢（新方向で）

- Primary red-yellow-blue のカラフル配色 — 子供向けアプリになる
- Neon（acid green + magenta 等）— 犬アルバムのブリーフに合わない、AI 既定 3 パターンの 2 番目「近黒 + acid green」に該当
- 手書き風フォント（Klee One 等）— 装飾で楽しさを語ってしまう（制約5）
- 多色のパステルレインボー — 写真と喧嘩する（制約2）
- Bootstrap 既定 `#dc3545` の危険赤 — palette から浮く
- Zen Maru Gothic / M PLUS Rounded 1c / Kosugi Maru / Klee One — 実測 90.8KB のコストがモバイル読者・週 1 回訪問という前提に合わない（上記 13 番）
- `-apple-system, BlinkMacSystemFont, "Hiragino Kaku Gothic ProN", "Yu Gothic", "Meiryo", sans-serif` の従来スタック — Windows で `"Yu Gothic"` が Light ウェイトになる問題があるため `system-ui` 先頭化 + `"Yu Gothic Medium"` 明示に置き換え（§3 フォントスタックの定義参照）

---

## 8. 影響範囲

### 8-1. 修正するファイル

| ファイル | 変更内容 | クライアント専用の根拠 |
|---|---|---|
| `src/resources/sass/client.scss` | **新規作成**。Bootstrap の `@import` を維持し、その上に §2 のカラートークン、§3 のタイポグラフィ、セッションカード・日付ブロック・フォーム調整を追加 | 新規ファイルのため他画面から未参照 |
| `src/resources/views/layouts/client.blade.php` | `@vite(['resources/sass/app.scss', ...])` → `@vite(['resources/sass/client.scss', 'resources/js/app.js'])` に差替。既存の `@stack('styles')` はそのまま利用 | `@extends('layouts.client')` は `client/login`, `client/dashboard`, `client/training-records/show` のみ（grep 済、8-2 参照） |
| `src/resources/views/layouts/client-public.blade.php` | 同上の vite 差替。加えて `@stack('styles')` を head 内に追加（現状無い） | `@extends('layouts.client-public')` は `client/password-setup/index`, `client/password-setup/invalid-token` のみ |
| `src/resources/views/layouts/client-intake.blade.php` | 同上の vite 差替。`@stack('styles')` 追加 | `@extends('layouts.client-intake')` は `client-intake/index-public`, `client-intake/complete-public`, `client-intake/errors/invalid-token` のみ |
| `src/app/Http/Controllers/Client/DashboardController.php` | **クエリ自体は既存のまま。取得後のビュー渡し用データ構造（`$mediaItems`）を「平坦化」から「記録ごとの入れ子」へ組み替える**。詳細は §8-1-1 | Client 名前空間配下、クライアントポータル専用のコントローラ |
| `src/resources/views/client/login.blade.php` | ワードマーク化、ロゴ画像撤去（差し替え可、§9-3）、フッター文言を外部供給の会社名または非表示（§9-2）に差替 | ファイル名が client 配下 |
| `src/resources/views/client/dashboard.blade.php` | セッションカード・フィードへ全面再構成。インライン `<style>` ブロック撤去（client.scss に移送）。JS の `alert()` を廃止。DashboardController が返す新しい構造に合わせる | ファイル名が client 配下 |
| `src/resources/views/client/training-records/show.blade.php` | ヘッダーブロック統合、担当1/担当2 廃止、空セクション非表示化、JS の `alert()` を廃止 | ファイル名が client 配下 |
| `src/resources/views/client/password-setup/index.blade.php` | ヘッダー文言変更、フォーム再組版（強度メーターは今回スコープ外、§4-7 参照） | ファイル名が client 配下 |
| `src/resources/views/client/password-setup/invalid-token.blade.php` | 「次にできること」ブロック追加 | ファイル名が client 配下 |
| `src/resources/views/client-intake/index-public.blade.php` | 3 点シェブロン化、ラベル縦積み化、`alert()` 廃止 | ファイル名が client-intake 配下、layouts.client-intake 経由でしか使われない |
| `src/resources/views/client-intake/complete-public.blade.php` | 文言差替 | 同上 |
| `src/resources/views/client-intake/errors/invalid-token.blade.php` | 「次にできること」ブロック追加 | 同上 |
| `vite.config.js` | エントリ配列に `resources/sass/client.scss` を追加。**共有される `public/build/manifest.json` が再生成される**ため、§8-1-2 のビルド手順・確認事項を必ず実施すること | エントリ追加自体は 1 行だが、副作用としてトレーナー側のバンドル参照解決に使われる manifest が更新される |

#### 8-1-1. DashboardController の変更内容

**既存クエリはそのまま使える。変更するのは取得後にビューへ渡すデータ構造だけ**。

現状の `index()` は以下のクエリで既にセッションカード・フィードに必要な情報を全て取得している：

```php
auth('client')->user()
    ->trainingRecords()
    ->with(['trainer1', 'trainer2', 'trainingType', 'mediaRecords'])
    ->withCount('mediaRecords')
    ->orderByDesc('training_date')
    ->get();
```

- **eager load のセット**（trainer1, trainer2, trainingType, mediaRecords）は既に存在する。N+1 は発生していない
- **`training_date` 降順ソート**も既に存在する
- **`withCount('mediaRecords')` は現状のまま残す**。`mediaRecords` が eager load 済みのため `$record->mediaRecords->count()` でも件数は取れるが、今回の目的はデザイン改良でありコントローラの整理ではない。差分を増やさない
- **クエリの起点は `TrainingRecord::` の静的呼び出しではなく `Client` の HasMany 経由**（`Client.php:142-145` に `trainingRecords(): HasMany` が定義されている）

##### 変更 1: `$mediaItems` を「平坦化」から「記録ごとの入れ子」へ組み替える

現状の `$mediaItems` は「記録×メディア」の**平坦配列**として組み立てられており、記録との紐付けが失われている。カード内スクロール廃止・記録単位表示のために、これを**記録ごとに入れ子の構造**に組み替える。

**「廃止する」のではなく、構造を変える**。理由：現状の `$mediaItems` 構築ループの過程で、各メディアについて以下の値がコントローラ側で生成されている。これらは Blade からモデルを辿るだけでは取得できないか、または Blade で呼ぶべきでない：

- `thumbnailUrl`: `$m->temporaryThumbnailUrl($thumbnailExpiresAt)` — **署名付き URL**。有効期限を渡して都度生成する必要があり、Blade ではなくコントローラで一括生成する
- `conversionStatus`: 「準備中です」表示（§6 文言書き換え表）の判定に必要
- `displayTitle`: `title` が空のとき `original_filename` にフォールバックするアクセサ値
- `type`: `photo` / `video` の分岐

これらは**引き続きコントローラで生成する**。有効期限の算出（現状の `$thumbnailExpiresAt`）もそのまま維持する。

概念的な返却構造（変数名・型は実装時に調整可）：

```
$sessions = [
  [
    'record' => $rec,          // TrainingRecord モデル（そのまま渡す）
    'media'  => [               // その記録のメディア配列（平坦化はしない）
      ['id' => ..., 'type' => ..., 'thumbnailUrl' => ...,
       'conversionStatus' => ..., 'displayTitle' => ...],
      ...
    ],
  ],
  ...
];
```

hero と feed への分割は、この `$sessions` を Blade 側で `first()` / `skip(1)` するか、コントローラで `$hero` / `$feed` として先に分けてから渡すか、いずれでもよい。実装時に判断する。

##### 変更 2: ノート表示に使うフィールドは `record_content` に確定する

`TrainingRecord` には `record_content` と `impression` の 2 つのテキストフィールドがある（`$fillable` で確認）。**セッションカードのノート冒頭（§4-2）と詳細画面のノート本体（§4-4）に使うのは `record_content` のみ**。

根拠：

1. **クライアント側 `client/training-records/show.blade.php` は現在 `record_content` を表示している**（line 76-77 該当行引用）：
   ```blade
   @if($trainingRecord->record_content)
       <div>{!! nl2br(e($trainingRecord->record_content)) !!}</div>
   ```
2. **`impression` はクライアント側のいずれの画面にも表示されていない**（`resources/views/client/` 配下を grep 済、0 件）。トレーナー側の登録・編集フォーム（`training-records/_form.blade.php`）と、トレーナー側詳細（`training-records/show.blade.php` の `section-impression` 折りたたみ）でのみ表示される
3. **決定的な根拠**: `src/app/Http/Controllers/Client/TrainingRecordController.php:16` のクラスコメントに以下の宣言がある：
   > 所感（impression）・最終更新者などクライアント非開示情報は

つまり `impression` は**設計上クライアント非開示のフィールド**。トレーナーの内部所見であり、飼い主に見せる判断は本プランの範囲外。

**禁止事項（明文化）**：

- セッションカードのノート冒頭・詳細画面のノート本体は **`record_content` のみを使用**する。`impression` を代替として使わない
- **現状クライアント側に表示していないフィールド（`impression`, `updated_by` 等）を、今回の再構成で新たに露出させない**。表示可否の判断は本プランの範囲外であり、必要になれば別プランで扱う

##### メディアが多対多である事実（実装者向けの注意）

`TrainingRecord::mediaRecords()` は **`belongsToMany`** であり、中間テーブル `media_record_training_record` を介する（`withPivot('sort_order')` / `orderByPivot('sort_order')`）。逆方向 `MediaRecord::trainingRecords()` も `belongsToMany`。

DashboardController.php:33 の既存コメントにあるとおり、**同じメディアが複数のトレーニング記録に紐づく場合がある**。フィード形式では**同じ写真が複数のセッションカードに現れ得る**。

これは仕様通りの挙動であり、今回の設計変更で対処すべき問題ではない。ただし実装者が「重複表示のバグ」と誤解して重複排除を入れてしまわないよう、事実として明示しておく。

#### 8-1-2. vite.config.js 変更の副作用と確認手順

- `public/build/manifest.json` は Vite が全エントリを一括管理する**単一ファイル**であり、トレーナー側の `app.blade.php`（`@vite(['resources/sass/app.scss', 'resources/js/app.js'])`）とクライアント側の `client.blade.php` などが**同じ manifest を参照**する
- エントリを追加すると manifest が**再生成される**。Hash 付きファイル名も変わり得る
- `public/build/` は Git 管理対象のため、**ビルド成果物を同一コミットに含める**こと。ビルドし忘れの状態を push すると本番でアセットの 404 が発生する
- 実装後、以下を必ず確認：
  1. `npm run build` を実行
  2. `public/build/manifest.json` の差分に `client.scss` エントリが追加され、既存の `app.scss` / `app.js` エントリのハッシュも同時に更新されていることを目視
  3. **トレーナー側の代表画面を目視確認**（ログイン・ダッシュボード・クライアント一覧・トレーニング記録一覧・トレーニング記録詳細）— スタイル崩れ・404 が出ないこと
  4. **クライアント側の代表画面を目視確認**（ログイン・ダッシュボード・トレーニング記録詳細・intake フォーム）
  5. `public/build/` 配下の生成物と manifest 差分を、Blade / SCSS の変更と**同一コミットに含めて**コミットする

### 8-2. 触らないファイル（明示）

以下は**共有**または**トレーナー側**の資産であり、本プランでは一切触れない。

| ファイル | 触らない理由 |
|---|---|
| `src/resources/sass/app.scss` | ブリーフ明示の制約。トレーナー側の全画面が依存 |
| `src/resources/js/app.js` | ブリーフ明示の制約 |
| `src/resources/views/layouts/app.blade.php` | トレーナー側の共通レイアウト |
| `src/resources/views/media-records/_lightbox.blade.php` | `training-records/show`（トレーナー側）と共用（grep 済） |
| `src/resources/views/media-records/_video-play-overlay.blade.php` | `training-records/show`（トレーナー側）、`media-records/index`（トレーナー側）と共用 |
| `src/resources/views/media-records/_video-play-overlay-styles.blade.php` | `training-records/_form`（トレーナー側）と共用 |
| favicon 一式 | ブリーフ明示の制約（全レイアウト共有） |

### 8-3. クライアント専用コピーの新設が「必要になったら」提案するファイル

現時点では**必須ではない**が、実装時に共有パーシャルの色調がクライアント側デザインから浮くと判断したら、クライアント専用コピーを提案する候補。

| 提案ファイル（未作成） | 判断基準 |
|---|---|
| `src/resources/views/client/media/_lightbox.blade.php` | 現状の共有ライトボックスは `rgba(0,0,0,0.88)` の黒背景。`ink`（#0F1A2E）に高い不透明度で置き換えたい場合のみ、コピーを提案する。MVP は共有のまま |
| `src/resources/views/client/media/_video-play-overlay.blade.php` | 共有の白 SVG ▶で十分。クライアント側で色を変えたい強い理由が出た場合のみ |
| `src/resources/views/client/media/_video-play-overlay-styles.blade.php` | 上に付随 |

これらは**任意の後続提案**であり、承認要素からは外す。

### 8-4. 将来の犬エンティティに向けた予約領域

現状のマークアップに、犬名・犬写真を後から差し込むための領域を残す。

- **ナビゲーション**: `[wordmark]` の右側または下に `<span class="dog-placeholder"></span>`（非表示クラス）を用意
- **ダッシュボード挨拶**: 「○○さん、こんにちは」の下に `<div class="dog-line"></div>` を用意
- **セッションカードの日付ブロック**: 日付の下に犬の顔写真（角丸小さめ）を差し込むためのスロットを CSS で用意（現状は空・0 高さ）

犬エンティティ追加時に、これらのスロットに `{{ $client->dog?->name }}` `{{ $client->dog?->photo_url }}` を差し込むだけで済むよう、CSS 変数と余白は今回のプランに含めておく。

---

## 9. 未決定事項（デザインではなく事業判断）

以下はデザイン判断ではなく事業判断のため、本プラン本文からは分離する。**プランはこれらが未確定でも成立する形**にしてある。確定した時点で該当箇所の外部供給値を差し替えるだけで反映できる。

### 9-1. プロダクト名

- 現時点で未確定。「しっぽ帳」は仮案であり本文の暫定表記としてのみ使う
- **Blade にベタ書きしない**。`config('app.client_portal_name')`（またはそれに準ずる config キー）を経由して供給する前提
- **ワードマーク（ログイン画面ヘッダー）は文字数で組みが変わる**ため、以下の想定範囲で成立させる：
  - 想定文字数: **3〜7 文字（日本語・漢字/ひらがな混在）**
  - システムサンス（ウェイト 700、色 cobalt）で 2.75rem 前後を基準に、7 文字までは 1 行、5 文字以下ではさらに大きく組む
  - Latin 表記が入る可能性（例: 英字プロダクト名）は当面想定しないが、混在時の縮約規則はフォールバックとして CSS の `max-width` + `text-wrap: balance` で吸収する
- 確定するまでは config 側にプレースホルダ値（例: `"クライアントポータル"`）を入れておき、Blade からは常に読み込む

### 9-2. フッターの会社名

- **表示するのはトレーニング提供会社名**（開発会社ではない）。前提が確定した
- 現時点で具体的な会社名は未確定
- 将来的に**複数のトレーニング会社が乗る可能性**があるため、Blade にベタ書きしない。config または DB からの外部供給を前提とする（どちらにするかは実装時に決定。プランでは「外部供給」とだけ扱う）
- **確定するまでフッターが空でもレイアウトが崩れない設計**にする（対象: ログイン画面、パスワード設定画面）：
  - 未供給時は `<footer>` 相当のブロックごと出力しない（空文字による空行が出ない）
  - フッターの上下余白は「上のカードから画面下までの視覚バランス」に依存させない（フッター行があってもなくても、上のカードの位置は同じ）
  - 実装上は Blade 側で `@if(config('app.client_portal_company'))` のようなガードを敷き、CSS ではカードを画面中央に絶対配置せず、上余白基準で配置する

### 9-3. ロゴ

- `public/images/client-portal-logo.png` は**仮のロゴ**。差し替え・撤去は可能（事実として確認済み）
- 本プランではワードマーク（テキストのみ）を主案とし、画像ロゴを使わない
- 将来ロゴ画像が確定した場合の再導入は、ワードマークの左に画像スロット（`max-height: 48px`）を追加するだけで済むよう、余白を予約しておく
- 差し替え時期・画像仕様は未定

---

## 承認要素（このプランで合意したいこと）

1. **方向性**: 「業務ツールではなく、犬の成長を届けるアルバム」— 写真が主役、UI は明るく元気な額縁
2. **カラー**: `paper / mat / ink / cobalt / persimmon / ember` の 6 トークン、および §2-0（写真と喧嘩させない規律）・§2-1 の色使用制約（persimmon は面 + ink 文字ペア専用、ember は mat 面のみ、フォーカスリングは mat + ink の二重輪郭、成功トーストは cobalt + mat 文字）
3. **書体**: システム日本語スタックのみ（Web フォント不採用、実測 90.8KB のコストを見送り）。見出しと本文の階層は書体差ではなく**ウェイト（見出し 700 / 本文 400）・サイズ・letter-spacing・色（見出しは cobalt）**で作る
4. **ダッシュボードの構造変更**: メディア独立ギャラリー廃止、セッションカード・フィードへ統合、カード内スクロール禁止。ページネーションは今回スコープ外
5. **シグネチャー**: 日付ブロック（**cobalt 帯 + mat 文字**、日本語縦組み `8月 / 2 / 金`、システムサンス 700）
6. **共通の文言方針**: 内部語彙（担当1／変換状態／メディア／未記入）を飼い主の言葉に置き換える
7. **実装スコープ**: `client.scss` 新規 + 6 View + 3 Layout + DashboardController + `vite.config.js`。共有パーシャル・app.scss・app.js・共有 media-records パーシャルは触らない
8. **スコープ外**: パスワード強度メーター（JS 実装方針が未定のため）／過去記録ページネーション（現状ペースで不要のため）／画像ロゴ再導入（ロゴ確定待ち）
9. **未確定事項の扱い**: プロダクト名・会社名・ロゴは外部供給で受け取る前提、未供給でもレイアウトが破綻しない構造にする
