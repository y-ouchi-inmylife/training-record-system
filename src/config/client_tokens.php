<?php

// クライアント向けトークンの有効期限設定
// 設計書: docs/architecture.md §3-1
return [

    /*
    |--------------------------------------------------------------------------
    | メールアドレス登録用 URL の有効日数
    |--------------------------------------------------------------------------
    |
    | トレーナーが発行する「メールアドレス登録用 URL」（DS-0700 client_email_registration_tokens.expires_at）
    | の有効日数。ログイン用リンク（DS-0600 client_password_setup_tokens.expires_at）は
    | この期限をそのまま引き継ぐため、専用の設定値は持たない。
    |
    | この期限が「初回設定完了までの全体の期限」となる。
    | お客様の操作で期限は延びない（トレーナーが URL を発行し直したときだけリセット）。
    |
    */
    'email_registration_expires_days' => 3,

    /*
    |--------------------------------------------------------------------------
    | メールアドレス確認リンクの有効日数（段階 4-3）
    |--------------------------------------------------------------------------
    |
    | ログイン後にメールアドレスを変更する際、新しいアドレスへ送るメールアドレス
    | 確認リンク（DS-0800 client_email_change_tokens.expires_at）の有効日数。
    | メールアドレス登録用 URL とは無関係に発行されるため、独立した設定値を持つ
    | （引き継ぐ元がない）。
    |
    */
    'email_change_confirm_expires_days' => 3,

    /*
    |--------------------------------------------------------------------------
    | パスワード再設定リンクの有効日数（段階 4-4）
    |--------------------------------------------------------------------------
    |
    | パスワードを忘れたお客様がログイン画面から申し込むパスワード再設定リンク
    | （DS-0900 client_password_reset_tokens.expires_at）の有効日数。
    | 他のトークン（メールアドレス登録用 URL・ログイン用リンク・メールアドレス確認リンク）
    | とは無関係に発行されるため、独立した設定値を持つ。
    |
    */
    'password_reset_expires_days' => 3,

];
